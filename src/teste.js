// Exportando constantes. 
const PI = 3.14;
const GTM_BRASIL = -3;
const nomes = ['Elias', 'Junior', 'Julia']

const atencao = nomes.map(nome => {
    return `Atenção ${nome}, `
})


export {
    PI,
    GTM_BRASIL,
    atencao
}